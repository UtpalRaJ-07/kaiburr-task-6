server.port=${PORT:8080}

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/kaiburr}

# choose execution mode: local or k8s
app.execution.mode=${EXECUTION_MODE:local}
app.k8s.namespace=${K8S_NAMESPACE:default}
app.k8s.image=${K8S_IMAGE:busybox:stable}
app.k8s.timeoutSeconds=${K8S_TIMEOUT_SECONDS:60}
